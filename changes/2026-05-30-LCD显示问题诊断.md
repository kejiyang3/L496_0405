# 变更：LCD 显示问题诊断（进行中）

## 原因
设备屏幕只亮背光，无显示内容。

## 诊断过程
- 对比 git diff：`app_lvgl.c` 二页 UI→验收 UI 重写，`Task_LVGL` 栈 8KB→4KB
- 新 `app_lvgl.c` 引用了旧版不存在的函数（`APP_LVGL_NotifyTouchActivity` 等）
- `Task_MSWriter` 优先级从 BelowNormal→Normal，可能抢占 LVGL

## 当前状态
- [x] 恢复 `app_lvgl.c` / `freertos.c` / `lv_port_disp.c` 到 HEAD 基线
- [x] 保留 LVGL 栈 8KB 修复
- [x] 编译通过
- [ ] 烧录验证（ST-Link 当前 Unknown 状态，需物理重连）

## 影响评估
- 硬件：否
- 固件：软件回退
- 外壳：否

## 下一步
- [ ] 物理重连 ST-Link 后烧录验证
- [ ] 如恢复后显示正常，逐步重新引入新 UI 代码

## 结果
- [ ] 🔶 进行中
