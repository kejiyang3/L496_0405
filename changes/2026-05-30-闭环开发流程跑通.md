# 变更：闭环开发流程跑通

## 原因
embed-ai-tool workflow 脚本存在多个兼容性问题，无法完成编译→烧录→监控闭环。

## 改动范围
- [x] `build/CMakeCache.txt` — 重新配置 cmake（路径过期）
- [x] `skills/embed-ai-tool/shared/tool_config.py` — 创建最小实现（缺失依赖）
- [x] `skills/embed-ai-tool/workflow/scripts/workflow_runner.py` — 移除 --flash 参数、--listen→--monitor、加 --no-verify、加 2s 延迟、改监控为 5s 定时
- [x] `skills/embed-ai-tool/flash-openocd/scripts/openocd_flasher.py` — 修复 detect_probes 误判、加 transport select swd、修复 subprocess.run 参数错位

## 影响评估
- 硬件：否
- 固件：否（仅修改开发工具脚本）
- 外壳：否
- 依赖：embed-ai-tool skill 脚本

## 验证方法
- [x] 编译通过
- [x] 烧录正常
- [x] 串口监控收到传感器初始化日志

## 回滚方案
pm install embed-ai-tool 重新安装原始 skill

## 结果
- [x] ✅ 成功 — 闭环命令跑通：编译 2.2s + 烧录 13s + 监控 5s
